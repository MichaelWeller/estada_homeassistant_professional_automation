configuration:
  log_level:
    name: Log level
  rules_path:
    name: Rules directory
  enable_ssh:
    name: Enable SSH/Remote-SSH access
  ssh_port:
    name: SSH port
  ssh_username:
    name: SSH username
  ssh_password:
    name: SSH password
  enable_debug:
    name: Enable Node Inspector
  debug_port:
    name: Debug port

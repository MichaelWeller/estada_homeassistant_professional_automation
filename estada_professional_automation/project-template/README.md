# Estada Professional Automation Rules

This folder contains your Home Assistant automation rules.

Rules are loaded from:

```text
/config/Estada_PA
```

Each `*.ts` file may default-export one rule class.

## Example

See `FirstAutomation.ts`.
